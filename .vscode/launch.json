{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "gdb",
            "request": "attach",
            "name": "Attach to QEMU",
            "executable": "./target/kernel",
            "target": ":1234",
            "remote": true,
            "gdbpath": "rust-gdb",
            "cwd": "${workspaceRoot}",
            "valuesFormatting": "prettyPrinters"
        }
    ],
}